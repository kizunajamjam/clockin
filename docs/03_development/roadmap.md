# ロードマップ

最終更新: 2026-06-18

---

## 凡例

- ✅ 完了
- 🚧 実装中 / 部分完了
- ⬜ 未着手
- 🔒 本番環境作業（意図的に後回し）

---

## フェーズ1：MVP コア（完了）

| 機能 | 状態 | 備考 |
|---|---|---|
| 認証（Supabase Auth） | ✅ | メール+パスワード |
| 組織・店舗・スタッフ管理 | ✅ | 招待リンク方式 |
| タブレット打刻（共有端末） | ✅ | スタッフ選択 + PIN |
| 個人スマホ打刻（/punch） | ✅ | |
| 1日複数回打刻 | ✅ | 部分ユニークインデックスで実現 |
| 勤怠記録閲覧・修正（管理者） | ✅ | |
| 給与計算（時給×実労働時間） | ✅ | |
| 深夜割増計算（22:00〜翌5:00 × 1.25倍） | ✅ | shop_staff.night_rate_included で制御 |
| 交通費（日払い/月払い） | ✅ | |
| CSV エクスポート（給与明細） | ✅ | |
| プロプラン判定（Stripe連携） | ✅ | subscriptions テーブル参照 |
| フリープランの5名制限 | ✅ | |

---

## フェーズ2：シフト・通知（完了）

| 機能 | 状態 | 備考 |
|---|---|---|
| シフト作成（管理者）| ✅ | 複数日一括入力、週/2週/月ビュー |
| シフト閲覧（スタッフ・/punch/shifts）| ✅ | |
| 週開始曜日設定（月曜/日曜）| ✅ | 店舗設定に移動、管理者のみ変更可 |
| 希望シフト提出（スタッフ）| ✅ | /punch/shifts → 「希望を出す」タブ |
| 希望シフト承認/却下（管理者）| ✅ | StaffShiftEditor 内で承認・却下ボタン |
| 希望シフト表示（カレンダーグリッド）| ✅ | バッジ表示（pending/approved/rejected 色分け）|
| 予定人件費表示（日別・期間合計）| ✅ | hourly_rate × シフト時間、深夜割増考慮 |
| プッシュ通知基盤 | ✅ | VAPID、service worker、push_subscriptions テーブル |
| 打刻完了後のプッシュ通知許可リクエスト | ✅ | PunchClient の result 画面 |
| AdSense バナー（フリープラン）| 🚧 | コンポーネント実装済み、publisher ID 未設定 |
| 年収アラート（累計到達通知）| ✅ | /punch/payroll に amber バナー表示 |

---

## フェーズ3：給与・精算（未着手）

| 機能 | 状態 | 備考 |
|---|---|---|
| カスタム給与項目 UI | ⬜ | DB スキーマは実装済み（salary_custom_items）|
| カスタム給与入力（管理者）| ⬜ | salary_custom_records |
| 給与明細 PDF 出力 | ⬜ | 候補: react-pdf |
| 控除計算（雇用保険・所得税）| ⬜ | 計算式は要件定義に記載済み |

---

## フェーズ4：本番・運用（後回し）

| 機能 | 状態 | 備考 |
|---|---|---|
| Cloudflare Pages デプロイ | 🔒 | `npm run deploy` で完結する予定 |
| Stripe Webhook 設定 | 🔒 | `STRIPE_WEBHOOK_SECRET` を設定する |
| AdSense 審査・設定 | 🔒 | 審査通過後に `NEXT_PUBLIC_ADSENSE_CLIENT_ID` 等を設定 |
| Supabase Pro 移行 | 🔒 | 月50K MAU 超えたら |
| ドメイン設定 | 🔒 | |

---

## フェーズ5：将来機能（検討中）

| 機能 | 状態 | 備考 |
|---|---|---|
| 複数店舗管理・横断集計 | ⬜ | DB は対応済み（organization単位）|
| マネージャーロール | ⬜ | shop_staff.role = 'manager' は定義済み |
| GPS 打刻（スマホ） | ⬜ | shops に gps_lat/lng/radius カラム済み |
| プッシュ通知の自動送信 | ⬜ | 出勤リマインダー（Supabase Edge Functions or cron）|
| LINE 連携 | ⬜ | MVP 後に検討 |
| iOS/Android ネイティブ | ⬜ | PWA で様子見 |
| アプリ名・ブランディング確定 | ⬜ | 仮称「clockin」|

---

## 直近の TODO（次のセッションで着手できるもの）

### 高優先
1. ~~**希望シフト → シフト自動反映ボタン**~~
   ✅ 2026-06-19 実装済み。`createShiftFromRequest`（承認済み希望を `shifts` に反映、時刻必須）＋
   ShiftCalendar に「シフトに反映」ボタン。

2. ~~**プッシュ通知の実際の送信トリガー**~~
   ✅ 2026-06-19 実装済み。承認/却下時（`updateShiftRequestStatus`）・確定反映時
   （`createShiftFromRequest`）に `sendPushToStaff()`。手動一括登録はスパム回避で対象外。
   残: 出勤リマインダー（cron/Edge Functionsによる時刻トリガー）は引き続き未実装（フェーズ5）。

3. **カスタム給与項目 UI**
   `salary_custom_items` / `salary_custom_records` のスキーマは完成済み。
   管理者が項目を登録 → 月次で金額入力 → 給与計算に加算する画面を作る

### 中優先
4. **勤怠修正の使いやすさ向上**
   現状は管理者が直接時刻を修正するだけ。スタッフが申請 → 管理者承認のフローがあると◎

5. **シフトコピー機能**
   先週のシフトを今週にまるごとコピーするボタン

6. **AdSense 本番設定**
   publisher ID 取得後に `.env.local` に追加するだけで動く

---

## 技術的負債・注意点

| 項目 | 内容 |
|---|---|
| ~~`punch/setup-complete` が残存~~ | ✅ 2026-06-19 削除済み（`/punch-complete/` に移行済み） |
| ~~キオスクPINのレート制限~~ | ✅ 2026-06-19 PINロックアウト実装（5回失敗で5分ロック / [review S-2](../04_review/review-2026-06-19.md)） |
| ~~service_role 依存の認可~~ | ✅ 2026-06-19 `src/lib/auth.ts` に認可ヘルパ集約（[review S-3](../04_review/review-2026-06-19.md)）。新規アクションも必ず経由すること |
| ~~テナント越境の書き込み（IDOR）~~ | ✅ 2026-06-19 第2回監査で発見・修正（R-1/R-3/R-4）。**更新/削除は必ず `shop_id`/`organization_id` でスコープする**ルールを徹底（[review §5](../04_review/review-2026-06-19.md)） |
| フリープランの店舗数制限 | ⬜ 未実装。課金画面は「フリー=1店舗」と表記するが `createShop` は無制限。収益方針として実装要否を判断（R-5） |
| `isPro` 判定の重複 | dashboard/billing/payroll/staff で個別にインライン判定。共通ヘルパ化が望ましい（任意） |
| attendances の `date` カラム | 元の UNIQUE 制約を前提にした `date` カラムは残存するが、現在は部分インデックスのみ有効。将来的に `date` カラム自体の役割を再整理したい |
| `calcShiftCost` の精度 | 深夜割増を 15 分刻みで近似計算。厳密な労基計算（秒単位）が必要なら修正要 |
| RLS ポリシーのテスト | Supabase RLS はローカルテストが難しい。本番前に各ロールで動作確認を手動で行うこと |
